name: linux_sys_ffi
description: Native Linux system interaction for Flutter and Dart. Easily manage background processes, network interfaces, hardware states via FFI, and complex FFmpeg media conversion natively.
version: 0.1.0
repository: https://github.com/ThanCoder/linux_sys_ffi.git

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  ffi: ^2.2.0
  # path: ^1.9.0

dev_dependencies:
  ffigen: ^15.0.0
  lints: ^6.0.0
  test: ^1.25.6
